启动框架
StartUp，毕竟是 Google 官方出品，现有的启动框架，或多或少都有参考 StartUp

一个合格的启动框架特性：
1.支持异步任务
2.支持组件化
3.支持任务依赖
4.支持优先级
5.支持多进程


将任务分级：
有依赖关系的任务按依赖关系分配好，需要优先执行的任务执行后，再执行后面任务

对任务再次进行分批，并发执行的任务可以并发执行。

1.支持异步任务和支持任务依赖和支持优先级可以通过下面方式体现
	执行一个任务前，先判断有没依赖的任务没有执行，先执行依赖的任务，到最后一个没有依赖任务执行前，判断下是否有可以并发执行的任务，如果有，开启线程池，并发执行，开启线程池并发执行的时候可能又会有依赖线程，依赖线程又按前面逻辑判断是否有依赖关系。。
	最终在一个执行依赖关系任务的线上面又使用线程池进行了并发执行操作，并发执行才能真正在启动时间上做到优化，谷歌推荐的StartUp只对有依赖关系的任务做了优化，
	对实际用户体验上不是很直观。
2.支持组件化
	可以使用APT工具在编译器动态收集任务的方式
	注解 javaPoet写入工具




